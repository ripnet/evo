{% macro att(name, value) %}{% if name == "swapXY" and value == 1 %} swapxy="true"{% else %}{% if value is not empty %} {{ name }}="{{ value }}"{% endif %}{% endif %}{% endmacro %}
{% macro axis(table, axis, attributes) %}{% import _self as xml %}{% for attrib in attributes %}{{ xml.att(attrib|lower, attribute(table, axis ~ 'Axis' ~ attrib)) }}{% endfor %}{% endmacro %}

